services:
  mysql:
    image: mysql:8.4
    environment: { MYSQL_DATABASE: beautyboy, MYSQL_ROOT_PASSWORD: local1234 }
    ports: ["3306:3306"]
    volumes: [mysql-data:/var/lib/mysql]
volumes: { mysql-data: }
