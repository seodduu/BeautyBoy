# 화장품 성분 주의도 점수 설계

## 0. 검증 결과 반영 (2026-07-25)

이 문서 초안의 방향은 유효하나, 식약처 공공데이터 API 2종을 **실제로 호출·전수 수집해 검증**한 결과 아래를 확정한다. 이 절이 이후 본문과 충돌하면 **이 절이 우선한다.**

### 0.1 확정된 데이터 출처 (실호출 완료)

- **원료성분정보**(한글↔INCI): `https://apis.data.go.kr/1471000/CsmtcsIngdCpntInfoService01/getCsmtcsIngdCpntInfoService01` — 총 21,833건, 영문명 보유 94%.
- **사용제한 원료정보**: `https://apis.data.go.kr/1471000/CsmtcsUseRstrcInfoService/getCsmtcsUseRstrcInfoService` — 총 31,191건이나 **국가별 다중 규제**라 `COUNTRY_NAME='한국'`만 사용(4,079행: 금지 3,083·한도 655·한도/금지 341).
- **인증키**: `.env`의 `COS_INFO`(64자, data.go.kr 일반 인증키). 이용허락범위 제한 없음.
- **필터 파라미터가 동작하지 않는다** — `COUNTRY_NAME`/`INGR_ENG_NAME`을 넘겨도 무시된다. 따라서 **전량 페이징 수집(사용제한 63p·원료 44p, numOfRows 최대 500) 후 로컬 조인**한다. 상세 필드는 [[mfds-ingredient-api]] 참조.

### 0.2 착향제 알레르기 유발물질 25종은 이 API 밖에 있다

25종은 **표시지정**이지 금지/한도가 아니라서 사용제한 API의 한국 데이터에 **없다**(다른 나라 행으로만 존재). 따라서 §3.3의 "자체 대표 주의성분 테이블"의 실체는 **고시 별표2 기반 25종 고정 리스트**이며, 한글 표준명·CAS는 원료성분 API에서 채운다(25종 중 23종 자동 확보, evernia prunastri·furfuracea 2종만 수동). 이 25종이 종합판정의 핵심 축이다.

### 0.3 §6.4·§7.2의 "한도 → 3단계"는 폐기한다 (실측 기반 정정)

cosmetics.csv 1,472개를 실제 한국 규제로 대조한 결과:

| 축 | 걸린 제품 | 처리 |
|---|---:|---|
| 금지 성분 포함 | 19% | 소싱 제외 또는 `검토 필요` (제품 위험이 아니라 데이터 확인 신호) |
| **한도 성분 포함** | **77%** | **종합판정에서 제외.** "배합한도가 있는 성분"이라는 정보 표시로만 사용 |
| 착향제 25종 포함 | 31% | `확인 필요` 축 (종합판정 주도) |
| 각질제거 산 포함 | 33% | `확인 필요` 축 (종합판정 주도) |

한도를 판정에 넣으면 **카탈로그 77%가 최고 경고**가 되어 신호가 죽는다. 페녹시에탄올·소듐하이드록사이드처럼 한도가 있으나 정상 사용되는 성분이 대부분이기 때문. 그리고 **CSV에는 실제 배합 함량이 없어** 한도 준수 여부를 계산할 수 없으므로(§13 참조) 한도는 애초에 위험도로 쓸 수 없다. → **종합판정 N = 착향제 개수 + 각질산 개수**, 씻어내는 제품이면 `N-1`.

### 0.4 종합판정 4단계 (UX 기획서와 통일)

`§9.3`의 6종 대신 UX 기획서의 4종으로 통일한다.

```
N=0 → 걱정 성분이 거의 없어요     N=1~2 → 대체로 무난해요
N=3~4 → 민감한 피부는 확인이 필요해요   N≥5 → 주의가 필요한 성분이 있어요
(금지 매칭 시 → 성분 정보 검토가 필요해요)
```

### 0.5 스키마 반영 범위 (V60, 추가형)

`ingredient.category`는 궁합 엔진(`CompatService`)·V50 루틴이 의존하므로 **유지**한다. `irritation_level`·`comedogenic`(근거 없는 수기 값)은 15개 파일이 참조하므로 **V60에서 컬럼은 남기되 사용 중단(deprecated)** 하고, DTO·프론트 배지의 플래그 전환은 별도 실행 단계로 분리한다. V60은 `inci_name`·`cas_no`·`kr_name` 추가와 `ingredient_flag` 신설까지다.

### 0.6 색상 — DESIGN.md 위반 정정

`§10.3`의 청록·파랑 5색은 폐기한다. DESIGN.md는 `signal-*` 5종 외 액센트와 배경 채움을 금지한다. → 확인필요=`signal-caution`, 검토필요=`signal-danger`, 그 외=중립(`stone`/`graphite`), **텍스트·아이콘·1px 테두리로만**.

---

## 1. 목적

화장품 성분에 익숙하지 않은 사용자가 개별 성분을 직접 공부하지 않아도, 제품을 구매하기 전에 확인할 내용을 빠르게 이해할 수 있도록 성분별 주의도를 `0~3단계`로 제공한다.

이 시스템은 의학적 진단이나 제품의 절대적인 안전성 판정을 목적으로 하지 않는다. 공식 규제 자료와 자체 기준을 사용해 **확인이 필요한 성분과 그 이유를 이해하기 쉽게 안내하는 것**을 목적으로 한다.

서비스에서는 `유해성 점수`보다 다음 명칭을 사용한다.

> 성분 주의도

또는:

> 성분 확인 단계

---

## 2. 핵심 원칙

### 2.1 유해성과 위해성은 구분한다

성분 자체에 자극이나 알레르기 가능성이 있더라도 실제 제품에서의 영향은 다음 조건에 따라 달라질 수 있다.

- 실제 배합 농도
- 씻어내는 제품인지 피부에 남는 제품인지
- 얼굴, 눈, 입술, 두피 등 사용 부위
- 사용 횟수와 사용량
- 사용자의 피부 타입과 알레르기
- 법정 사용 한도 준수 여부

따라서 성분명만으로 `위험하다`, `안전하다`라고 단정하지 않는다.

### 2.2 0단계는 안전 보증이 아니다

`0단계`는 해당 성분이 완전히 안전하다는 의미가 아니다.

> 현재 적용한 공식 기준에서 별도의 사용 제한 또는 주의 근거가 확인되지 않음

위 의미로 사용한다.

### 2.3 금지 원료는 점수를 부여하지 않는다

금지 원료가 확인되면 `3단계`로 표시하지 않는다. 해당 상품을 일반적인 평가 대상에서 제외하고 데이터 또는 상품 정보 검토 상태로 전환한다.

```text
일반 원료       → 0~2단계
사용 제한 원료  → 조건에 따라 2~3단계
금지 원료       → 점수 없음, 검토 필요
식별 실패 성분  → 점수 없음, 정보 부족
```

### 2.4 제품 점수는 성분 점수의 합이 아니다

성분 점수를 모두 더하면 성분 수가 많은 제품일수록 불리해진다. 제품의 종합 판정은 가장 높은 주의도, 주의 성분 개수, 제품 유형과 사용자 조건을 함께 사용한다.

---

## 3. 데이터 출처

### 3.1 식품의약품안전처 화장품 원료성분정보 API

- 링크: [공공데이터포털 화장품 원료성분정보](https://www.data.go.kr/data/15111774/openapi.do)
- 제공 방식: REST API
- 데이터 형식: JSON, XML
- 이용료: 무료
- 공공데이터포털 표시 기준 이용허락범위: 제한 없음

주요 사용 항목:

- 표준명
- 영문명
- CAS 번호
- 기원 및 정의
- 이명

사용 목적:

- 상품 전성분 표기의 표준화
- 한글명과 INCI 영문명 연결
- 동일 성분의 이명 처리
- CAS 번호를 통한 정확한 성분 식별

### 3.2 식품의약품안전처 화장품 사용제한 원료정보 API

- 링크: [공공데이터포털 화장품 사용제한 원료정보](https://www.data.go.kr/data/15111772/openapi.do)
- 제공 방식: REST API
- 데이터 형식: JSON, XML
- 이용료: 무료
- 공공데이터포털 표시 기준 이용허락범위: 제한 없음

주요 사용 항목:

- 표준명
- 영문명
- CAS 번호
- 배합 제한
- 고시 원료명
- 단서 조항
- 제한사항

사용 목적:

- 법적 사용 제한 여부 확인
- 배합 한도 확인
- 제품 유형, 사용 부위, 연령 등 제한 조건 확인
- 성분 기본 주의도 산정

### 3.3 자체 대표 주의성분 테이블

식약처 원료 API와 사용제한 API만 사용하면 대부분의 일반 성분은 `0단계`로 분류된다. 알레르기, 향 민감성, 민감 피부 참고사항처럼 사용제한 목록에 구조화되어 있지 않은 정보를 제공하려면 대표 주의성분 테이블을 별도로 관리한다.

포트폴리오 단계에서는 대표 성분 약 20~50개를 공식 자료를 바탕으로 직접 정리한다.

보조 자료로 사용할 수 있는 출처:

- 식품의약품안전처 고시 및 안전성 자료
- 식품의약품안전평가원 자료
- EU SCCS 과학적 의견
- EU CosIng

EU CosIng은 보조적인 정보 데이터베이스이며 그 자체가 법적 승인 목록이나 법적 판단은 아니므로, 국내 서비스의 1차 기준은 식약처 자료로 설정한다.

---

## 4. 상품 전성분 확보 방법

공공 API는 원료에 관한 정보는 제공하지만 시중 모든 상품의 최신 전성분을 완전하게 제공하는 상품 데이터베이스는 아니다.

상품별 전성분은 다음 방법으로 확보한다.

### 실제 서비스

- 판매자 또는 제조사가 입점 단계에서 직접 입력
- 전성분 표시 사진 제출
- 제품 유형 및 사용 방법 제출
- 제한 성분이 있다면 배합 농도 또는 기준 준수 여부 제출

### 포트폴리오

- 데모 상품 데이터를 직접 작성
- 실제 포장에 표시된 전성분을 수동 입력
- 제품 상세 데이터에 전성분 배열을 포함

상품 입력 데이터 예시:

```json
{
  "productId": "cleanser-001",
  "name": "퓨어그루브 딥클렌징폼",
  "category": "CLEANSER",
  "applicationArea": ["FACE"],
  "rinseOff": true,
  "targetSkinTypes": ["OILY"],
  "ingredients": [
    "정제수",
    "글리세린",
    "살리실산",
    "향료"
  ]
}
```

---

## 5. 성분 식별 및 정규화

상품의 전성분 표기와 식약처 API의 성분명이 정확히 일치하지 않을 수 있으므로 정규화 과정이 필요하다.

### 5.1 권장 매칭 우선순위

1. CAS 번호 완전 일치
2. 영문 표준명 완전 일치
3. 한글 표준명 완전 일치
4. 등록된 이명 완전 일치
5. 수동 매핑 테이블 확인
6. 일치하지 않으면 `정보 부족` 처리

유사 문자열 검색만으로 자동 확정하지 않는다. 잘못된 성분에 높은 주의도를 부여하거나 실제 제한 성분을 놓칠 수 있기 때문이다.

### 5.2 정규화 예시

```text
입력: Fragrance
표준 성분명: 향료
연결된 이명: Parfum, Aroma
식별 기준: 영문명 또는 수동 매핑
```

성분 마스터 예시:

```json
{
  "id": "ingredient-fragrance",
  "koreanName": "향료",
  "inciName": "PARFUM",
  "casNumbers": [],
  "aliases": ["Fragrance", "Parfum", "Aroma"]
}
```

---

## 6. 성분 주의도 0~3단계

| 단계 | 화면 표현 | 기준 |
|---:|---|---|
| 0 | 별도 제한 정보 없음 | 현재 적용한 공식 자료에서 별도의 제한 또는 주의 근거가 확인되지 않음 |
| 1 | 참고 | 특정 피부 타입, 알레르기 또는 사용 조건에서 참고할 내용이 있음 |
| 2 | 주의 | 사용 부위, 제품 유형, 연령 또는 대상에 관한 제한이나 명확한 주의 정보가 있음 |
| 3 | 제한 | 배합 한도 등 명확한 법적 사용 제한이 있음 |
| 점수 없음 | 정보 부족 | 성분 식별 또는 평가 근거가 부족함 |
| 점수 없음 | 검토 필요 | 금지 원료 가능성 또는 데이터 충돌이 있음 |

### 6.1 0단계

적용 조건:

- 성분이 정상적으로 식별됨
- 사용제한 원료 목록과 일치하지 않음
- 자체 주의성분 테이블에 별도 근거가 없음

권장 문구:

> 현재 기준에서 별도 제한 정보가 확인되지 않아요.

금지 문구:

> 완전히 안전한 성분이에요.

### 6.2 1단계

적용 조건:

- 법적 사용 제한은 확인되지 않음
- 공식 자료를 근거로 특정 피부 타입이나 알레르기 보유자가 참고할 내용이 있음
- 자체 대표 주의성분 테이블에 등록되어 있음

권장 문구:

> 향에 민감한 사용자는 확인해보세요.

### 6.3 2단계

적용 조건:

- 특정 사용 부위 제한
- 씻어내는 제품 또는 피부에 남는 제품에 따른 조건
- 특정 연령 또는 사용자 대상 제한
- 제품 유형에 따른 사용 조건

권장 문구:

> 제품 유형과 사용 부위에 따라 확인이 필요한 성분이에요.

### 6.4 3단계

> ⚠️ **§0.3에 의해 정정됨.** 초안은 "배합 한도가 있으면 3단계"였으나, 실측 결과 카탈로그 77%가 한도 성분을 포함해 신호가 죽는다. **한도는 성분 주의도 단계를 올리지 않는다.** 아래는 초안 기록으로 남긴다.

적용 조건:

- 배합 가능한 최대 농도가 정해져 있음
- 법적 사용 한도 또는 명시적인 배합 조건이 있음
- 실제 제품 농도를 확인해야 적합 여부를 판단할 수 있음

한도 성분은 단계가 아니라 **성분 카드의 부가 정보**로만 표시한다.

권장 문구:

> 이 성분은 배합한도가 정해져 있어요. 실제 함량은 표시되지 않아 확인할 수 없어요.

법정 한도 내에서 사용된 제한 원료를 위험한 제품으로 오해하게 만들지 않는다.

---

## 7. 점수 계산 로직

### 7.1 기본 처리 순서

```text
상품 전성분 입력
    ↓
성분명 정규화
    ↓
식약처 원료성분정보와 매칭
    ↓
식약처 사용제한 원료정보 조회
    ↓
자체 주의성분 테이블 조회
    ↓
제품 유형 및 사용자 조건 적용
    ↓
최종 0~3단계 산정
```

### 7.2 기본 의사 코드

```ts
type IngredientLevel = 0 | 1 | 2 | 3 | null;

interface IngredientAssessment {
  level: IngredientLevel;
  status:
    | "NO_RESTRICTION"
    | "REFERENCE"
    | "CAUTION"
    | "RESTRICTED"
    | "INSUFFICIENT_DATA"
    | "REVIEW_REQUIRED";
  reasons: string[];
}

function calculateIngredientLevel(
  ingredient,
  product,
  userProfile
): IngredientAssessment {
  if (!ingredient.matched) {
    return {
      level: null,
      status: "INSUFFICIENT_DATA",
      reasons: ["공식 원료 데이터에서 성분을 식별하지 못했어요."]
    };
  }

  if (ingredient.prohibited) {
    return {
      level: null,
      status: "REVIEW_REQUIRED",
      reasons: ["금지 원료 가능성이 있어 데이터 검토가 필요해요."]
    };
  }

  let level: 0 | 1 | 2 | 3 = 0;
  const reasons: string[] = [];

  if (ingredient.hasOfficialCaution) {
    level = Math.max(level, 1) as 0 | 1 | 2 | 3;
    reasons.push(ingredient.cautionReason);
  }

  if (
    ingredient.hasUsageRestriction ||
    ingredient.hasBodyAreaRestriction ||
    ingredient.hasAgeRestriction ||
    ingredient.hasProductTypeRestriction
  ) {
    level = Math.max(level, 2) as 0 | 1 | 2 | 3;
    reasons.push("사용 대상 또는 제품 유형에 관한 조건이 있어요.");
  }

  // §0.3 정정: 배합 한도는 단계를 올리지 않는다. 성분 카드 부가정보로만 기록한다.
  if (ingredient.hasConcentrationLimit) {
    reasons.push("배합한도가 정해진 성분이에요(실제 함량은 표시되지 않음).");
    // level은 변경하지 않는다.
  }

  if (
    userProfile?.skinType === "SENSITIVE" &&
    ingredient.sensitiveSkinCaution
  ) {
    level = Math.min(3, level + 1) as 0 | 1 | 2 | 3;
    reasons.push("민감성 피부가 확인할 성분이에요.");
  }

  if (product.rinseOff && ingredient.rinseOffModifierAllowed) {
    level = Math.max(0, level - 1) as 0 | 1 | 2 | 3;
    reasons.push("씻어내는 제품이라는 점을 반영했어요.");
  }

  const statusMap = {
    0: "NO_RESTRICTION",
    1: "REFERENCE",
    2: "CAUTION",
    3: "RESTRICTED"
  } as const;

  return {
    level,
    status: statusMap[level],
    reasons
  };
}
```

### 7.3 보정 로직 사용 시 주의사항

씻어내는 제품이라는 이유만으로 식약처의 법적 제한 단계 자체를 낮추면 안 된다.

다음 두 값을 분리하는 것이 가장 안전하다.

- `regulatoryLevel`: 공식 규제 기준 단계
- `displayLevel`: 제품 유형과 사용자 피부 타입을 고려한 안내 단계

```json
{
  "regulatoryLevel": 3,
  "displayLevel": 2,
  "reason": "배합 한도가 있는 성분이며, 해당 제품은 씻어내는 형태입니다."
}
```

공식 규제 단계는 변경하지 않고, 제품 맥락은 설명과 화면 표시에서 별도로 반영한다.

---

## 8. 자체 대표 주의성분 테이블

예시 구조:

```json
{
  "ingredientId": "ingredient-fragrance",
  "baseLevel": 1,
  "concerns": [
    "FRAGRANCE_SENSITIVITY",
    "ALLERGY"
  ],
  "sensitiveSkinCaution": true,
  "rinseOffModifierAllowed": false,
  "reason": "향에 민감하거나 알레르기가 있는 사용자는 확인이 필요해요.",
  "sources": [
    {
      "organization": "공식 기관명",
      "title": "자료 제목",
      "url": "https://example.com/official-source",
      "reviewedAt": "2026-07-24"
    }
  ],
  "methodologyVersion": "1.0.0"
}
```

각 데이터에는 반드시 다음을 저장한다.

- 평가 근거
- 원문 출처 URL
- 검토 날짜
- 적용된 평가 기준 버전
- 점수를 입력하거나 검토한 사람

---

## 9. 제품 종합 판정

### 9.1 단순 합산을 사용하지 않는 이유

다음 상품을 가정한다.

```text
상품 A: 1단계 성분 2개 → 합계 2점
상품 B: 1단계 성분 10개 → 합계 10점
```

단순 합계에서는 상품 B가 매우 위험한 제품처럼 보이지만, 실제로는 1단계 참고 성분의 개수가 많을 뿐이다. 따라서 제품 판정은 점수 합계가 아니라 단계별 분포를 사용한다.

### 9.2 권장 판정 입력값

- 가장 높은 성분 주의도
- 3단계 성분 개수
- 2단계 성분 개수
- 1단계 성분 개수
- 정보 부족 성분 개수
- 씻어내는 제품 여부
- 사용자 피부 타입

### 9.3 제품 판정 의사 코드

```ts
function calculateProductVerdict(assessments) {
  const scored = assessments.filter((item) => item.level !== null);
  const unknownCount = assessments.filter(
    (item) => item.status === "INSUFFICIENT_DATA"
  ).length;
  const reviewRequired = assessments.some(
    (item) => item.status === "REVIEW_REQUIRED"
  );

  if (reviewRequired) {
    return {
      type: "REVIEW_REQUIRED",
      title: "성분 정보 검토가 필요해요",
      description: "공식 데이터와 일치하지 않는 성분이 있어요."
    };
  }

  if (unknownCount > 0) {
    return {
      type: "INCOMPLETE",
      title: "일부 성분의 정보가 부족해요",
      description: `${unknownCount}개 성분을 확인하지 못했어요.`
    };
  }

  const countByLevel = {
    3: scored.filter((item) => item.level === 3).length,
    2: scored.filter((item) => item.level === 2).length,
    1: scored.filter((item) => item.level === 1).length
  };

  if (countByLevel[3] > 0) {
    return {
      type: "CHECK_CONDITIONS",
      title: "사용 조건을 확인해보세요",
      description: `배합 또는 사용 조건이 있는 성분이 ${countByLevel[3]}개 있어요.`
    };
  }

  if (countByLevel[2] > 0) {
    return {
      type: "CAUTION",
      title: "민감한 피부는 확인이 필요해요",
      description: `주의 성분이 ${countByLevel[2]}개 있어요.`
    };
  }

  if (countByLevel[1] > 0) {
    return {
      type: "MOSTLY_FINE",
      title: "대체로 무난해요",
      description: `참고할 성분이 ${countByLevel[1]}개 있어요.`
    };
  }

  return {
    type: "NO_RESTRICTION_FOUND",
    title: "별도로 확인된 제한 성분이 없어요",
    description: "현재 적용한 공식 기준으로 확인했어요."
  };
}
```

---

## 10. 화면 표시 방법

### 10.1 상품 상세페이지 요약 카드

```text
● 대체로 무난해요

참고할 성분 2개와 주의 성분 1개가 있어요.
이 제품은 씻어내는 클렌저예요.

[주의 성분 3개 확인 →]
```

### 10.2 주의 성분 패널

```text
주의 성분 3개                                  ×

민감하거나 건조한 피부라면 확인해보세요.
이 제품은 씻어내는 클렌저입니다.

살리실산                                  주의도 2

왜 들어갔나요?
각질과 피지를 정리하기 위해 사용해요.

누가 확인해야 하나요?
민감하거나 건조한 피부는 자극을 느낄 수 있어요.

이 제품에서는?
씻어내는 제품이라는 점을 함께 고려했어요.

[평가 근거 보기]
```

### 10.3 단계별 색상

- 0단계: 중립적인 회색 또는 청록색
- 1단계: 밝은 청록색 또는 파란색
- 2단계: 주황색
- 3단계: 진한 주황색
- 검토 필요: 빨간색

일반 성분을 모두 초록색으로 표시하면 `안전 보증`처럼 보일 수 있으므로, 0단계는 중립색을 사용하는 것이 좋다.

---

## 11. 권장 문구와 금지 문구

| 피해야 할 문구 | 권장 문구 |
|---|---|
| 안전한 성분 | 별도 제한 정보가 확인되지 않은 성분 |
| 위험 성분 | 확인이 필요한 성분 |
| 유해성 3점 | 성분 주의도 3단계 |
| 이 제품은 안전합니다 | 현재 기준에서 별도 제한 성분이 확인되지 않았어요 |
| 이 성분은 피부에 해롭습니다 | 특정 피부 타입이나 사용 조건에서 주의가 필요할 수 있어요 |
| 고위험 성분 Free | 고위험 판정 성분 0개 |

---

## 12. 데이터베이스 구조 예시

### ingredients

```text
id
korean_name
inci_name
cas_number
definition
aliases
source_updated_at
```

### ingredient_restrictions

```text
id
ingredient_id
restriction_type
concentration_limit
product_type_condition
application_area_condition
age_condition
legal_text
source_url
effective_date
```

### ingredient_cautions

```text
id
ingredient_id
base_level
concern_type
reason
sensitive_skin_caution
rinse_off_modifier_allowed
source_url
reviewed_at
methodology_version
```

### product_ingredients

```text
product_id
ingredient_id
display_order
raw_name
matched_by
match_confidence
```

### ingredient_assessments

```text
product_id
ingredient_id
regulatory_level
display_level
status
reasons
methodology_version
calculated_at
```

---

## 13. 예외 처리

### 성분을 식별하지 못한 경우

```text
점수: 표시하지 않음
상태: 정보 부족
문구: 공식 원료 데이터에서 이 성분을 확인하지 못했어요.
```

### 동일 이름이 여러 성분을 의미하는 경우

자동 평가하지 않고 CAS 번호 또는 제조사 정보를 추가로 확인한다.

### 금지 원료와 일치하는 경우

```text
점수: 표시하지 않음
상태: 검토 필요
상품 종합 판정: 성분 정보 검토 중
```

### 배합 한도를 알 수 없는 경우

```text
단계: 3단계
문구: 배합 한도가 정해진 성분이지만 실제 함량은 확인할 수 없어요.
```

함량을 모르는 상황에서 규정 위반이라고 단정하지 않는다.

---

## 14. 데이터 갱신 및 버전 관리

식약처 데이터와 자체 평가 기준은 변경될 수 있으므로 다음 정보를 관리한다.

- API 데이터 동기화 일시
- 식약처 제한 기준 시행일
- 자체 평가 기준 버전
- 성분별 마지막 검토일
- 평가 결과 생성일

화면의 평가 근거 영역에는 다음처럼 표시한다.

```text
식약처 공개자료 기준
데이터 업데이트: 2026.07.24
평가 기준 버전: 1.0.0
```

평가 기준이 변경되면 기존 상품도 다시 계산한다.

---

## 15. 안내 문구

상품 상세페이지 또는 주의 성분 패널 하단에 다음 문구를 제공한다.

> 성분 주의도는 식약처 공개자료와 자체 평가 기준을 바탕으로 제공하는 참고 정보입니다. 실제 사용 경험은 배합 농도, 사용 방법 및 개인의 피부 상태에 따라 달라질 수 있으며, 의학적 진단이나 제품 안전성 보증을 의미하지 않습니다.

포트폴리오임을 명시할 수 있다면 다음 문구도 추가한다.

> 이 페이지는 포트폴리오용 데모이며 표시된 상품 및 성분 평가는 실제 판매 또는 의학적 판단을 목적으로 하지 않습니다.

---

## 16. MVP 구현 범위

포트폴리오에서는 다음 범위로 구현한다.

1. 식약처 원료성분정보 API 연동
2. 식약처 사용제한 원료정보 API 연동
3. 데모 상품 5~10개 등록
4. 대표 주의성분 20~50개 자체 테이블 구축
5. 성분명 정규화 및 매칭
6. 성분별 0~3단계 계산
7. 정보 부족 및 검토 필요 예외 처리
8. 제품 종합 판정 생성
9. 상품 상세페이지 성분 요약 카드
10. 데스크톱 사이드 패널 및 모바일 바텀시트
11. 평가 근거와 데이터 기준일 표시

### MVP 성공 기준

- 데모 상품의 모든 입력 성분이 평가 또는 정보 부족 상태로 처리된다.
- 제한 원료는 식약처 데이터와 연결된다.
- 금지 원료는 숫자 점수로 표시되지 않는다.
- 제품 점수는 개별 성분 점수의 단순 합으로 계산되지 않는다.
- 사용자가 주의 성분의 이유와 출처를 확인할 수 있다.
- 0단계가 안전 보증처럼 표현되지 않는다.

---

## 17. 최종 결론

포트폴리오에서는 다음 구성으로 대부분의 화장품 성분을 `0~3단계`로 분류할 수 있다.

```text
식약처 화장품 원료성분정보 API
        +
식약처 화장품 사용제한 원료정보 API
        +
공식 출처를 근거로 직접 만든 대표 주의성분 테이블
        ↓
성분별 0~3단계 주의도
        ↓
최고 주의도와 단계별 개수를 이용한 제품 종합 판정
```

식약처 API만 사용하면 대부분의 일반 성분은 `0단계`가 된다. 이는 잘못된 결과가 아니지만 사용자에게 충분한 정보를 제공하기 어렵기 때문에, 대표적인 민감성·알레르기 관련 성분은 공식 자료를 바탕으로 자체 보조 테이블에 추가한다.

최종 화면에서는 점수 자체보다 다음 세 가지를 우선한다.

1. 왜 주의가 필요한가?
2. 누가 확인해야 하는가?
3. 이 제품에서는 어떻게 해석해야 하는가?

