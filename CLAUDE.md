# 뷰티보이 (BeautyBoy)

올리브영 클론 기반 **남성 화장품 커머스 플랫폼**. 취업 포트폴리오 프로젝트로,
"완성도 있는 범위 + 설계 이유를 설명할 수 있는 것"을 최우선 가치로 둔다.

- 설계: `docs/superpowers/specs/2026-07-23-beautyboy-design.md` — 도메인·API·범위의 유일한 진실
- 구현 계획: `docs/plans/` 아래 계획서 — 태스크·터미널 운용의 유일한 진실 (아직 없음, 계획 단계에서 생성)

스택: React SPA (Vite + TypeScript) / Spring Boot 모듈러 모놀리스 / MySQL / (선택) Redis.
현재 상태: **초기 셋팅 단계** — 설계 문서만 존재하고 코드는 아직 없다.

## 실행 모델

터미널 1개 = 독립 태스크 세트 1개 = worktree 1개 = 브랜치 1개.
각 터미널의 Claude Code 세션은 오케스트레이터이며, 태스크마다 서브에이전트를 스폰해 실행하고
사이사이 리뷰(테스트 통과 + 파일 소유권 준수)한다.

## 모델 배분 (서브에이전트 스폰 시 model 파라미터로 지정)

| 역할 | 모델 |
|---|---|
| 오케스트레이터 세션 (리뷰·머지 게이트) | opus |
| 태스크 실행 서브에이전트 — 기본 (테스트+구현 모두) | **sonnet** |
| 예외: 결제(2단계 검증)·오늘드림 재고 차감·성분 궁합 규칙 엔진 | **opus** |

예외의 근거: 이 세 곳은 계획서에 코드로 못 박기 어려운 판단(동시성, 정합성, 규칙 충돌 해석)이
필요하다. 나머지 태스크는 계획서에 사양이 명확히 적히므로 sonnet으로 충분하다.

## 설계·구현 계획 작성 규칙 (매 계획 공통)

설계 문서나 구현 계획을 만들 때 별도 지시 없이 항상 아래 구조로 작성한다.

1. 구현·테스트 작업은 서브에이전트가 실행하고, 메인 세션은 오케스트레이터(스폰·리뷰·머지 게이트)로 둔다. (모델 배분은 위 표 따름)
2. 병렬 가능한 태스크는 웨이브/의존성을 분석해 **터미널 분할**을 계획서에 명시한다. (터미널 1개 = worktree 1개 = 브랜치 1개)
3. 터미널을 분할하는 경우, **각 터미널에서 그대로 붙여넣어 쓸 실행 프롬프트**를 계획서 안에 함께 생성한다.
   프롬프트에는 **worktree 생성 명령어(`git worktree add <경로> <브랜치>`)를 반드시 포함**한다 —
   프롬프트만 있고 진입 명령이 없으면 터미널마다 브랜치를 즉흥으로 만들게 되고, 그때 브랜치 기점이
   어긋난다. 아래 순서를 계획서에 그대로 적는다.

   ```bash
   # 1) 계획서를 먼저 커밋한다 (untracked면 새 worktree에 딸려가지 않는다)
   git add docs/plans/<계획서>.md && git commit -m "docs(plan): ..."
   # 2) 그 커밋을 기점으로 각 터미널 worktree를 만든다
   git worktree add ../뷰티보이-<세트> <브랜치명>
   ```

   **브랜치 기점 확인은 필수**: 모든 터미널의 브랜치가 계획서 커밋을 포함해야 한다.
   계획서 커밋이 이미 특정 feature 브랜치에 들어가 있으면 다른 터미널은 그것을 못 보므로,
   해당 커밋에서 브랜치를 따거나 cherry-pick 한다.

## 모든 세션·서브에이전트 공통 규칙

- 자기 태스크의 Files 목록 밖 파일 수정 금지. 특히 **DB 마이그레이션(Flyway) 스크립트,
  공통 응답/에러 타입, 루트 빌드 설정(`build.gradle`, `package.json`, `vite.config.ts`)은
  공유 계약** — 안 맞으면 수정하지 말고 보고.
- 설계 문서와 실제 스키마/시드 데이터가 다르면 **스키마·픽스처가 진실** — 코드를 그쪽에 맞춘다.
- **패키지 = 서비스 경계.** 도메인 패키지(`member` `catalog` `ingredient` `search` `cart`
  `order` `payment` `review` `ranking` `delivery` `routine`)는 자기 테이블만 접근하고,
  타 도메인은 서비스 인터페이스를 경유한다. 타 도메인 엔티티/리포지토리 직접 import 금지.
- **돈과 재고는 서버, 취향은 클라이언트.** 가격·재고·주문·쿠폰 검증은 항상 서버에서 재검증한다.
  클라이언트 위임은 "틀려도 사고 안 나는" 계산(추천 정렬, 루틴 개인화, 궁합 진단)만.
- 주문 시점 데이터는 **스냅샷**으로 저장한다 (가격, 상품명, 배송지, 리뷰의 피부타입).
  참조로 남겨 나중에 값이 바뀌게 만들지 않는다.
- 시크릿(토스페이먼츠 테스트 키, DB 비밀번호, JWT 시크릿)은 코드·커밋에 넣지 않는다.
  환경변수 또는 `.env.local` / `application-local.yml`(gitignore)로만 주입한다.
- 유닛테스트는 인메모리/테스트컨테이너 DB + 픽스처만 사용 (터미널 병렬 안전).
  실제 외부 자원(공유 MySQL, 토스 테스트 API 호출)을 만지는 통합·E2E는 **직렬 전용 웨이브**로 분리한다.
- 커밋 메시지·문서·주석은 한국어. 커밋은 태스크 단위로 원자적으로.
