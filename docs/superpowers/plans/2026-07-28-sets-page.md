# 세트 비교 페이지 `/sets` 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `/main`의 세트 탭을 걷어내 원래 화면으로 되돌리고, 세트 3개를 세로로 나란히 비교하는 전용 페이지 `/sets`를 만든다.

**Architecture:** `useComposer(slug)`를 세트별 1회씩 총 3회 호출해 밴드 3개를 만든다. 컨셉 파생(`deriveSetConcepts`)과 조합 로직은 선행 구현 자산을 그대로 쓰고, 일괄 담기 로직만 공용 함수로 추출해 두 화면이 공유한다. 백엔드 변경 0.

**Tech Stack:** React 18 + TypeScript + react-router + @tanstack/react-query + vitest/@testing-library + msw (신규 의존성 없음)

스펙: `docs/superpowers/specs/2026-07-28-sets-page-design.md`
선행 스펙(컨셉 파생·조합 로직의 진실): `docs/superpowers/specs/2026-07-28-concept-sets-design.md`

## Global Constraints

- 프론트만 수정. 백엔드·Flyway·공통 응답/에러 타입·루트 빌드 설정(`build.gradle`, `package.json`, `vite.config.ts`) 접근 금지.
- `composer.ts`·`profile.ts`·`steps.ts`·`setConcepts.ts`는 **읽기 전용** (테스트 추가는 허용, 구현 수정 금지).
- CSS는 `DESIGN.md` 토큰 참조만. **hex 손 복사 금지**, 액센트(`signal-*`) 배경 금지, 한글 `word-break: keep-all`. 문서에 없는 사양이 필요하면 DESIGN.md에 먼저 추가(Task 3이 그 절차다).
- 개별 테스트: `cd frontend && npx vitest run <파일경로>`. **전체 판정은 `cd frontend && npm test`** — `npx vitest run` 단독은 e2e 스펙을 집어 항상 1 failed(거짓 적신호).
- 타입 게이트: `cd frontend && npx tsc --noEmit -p tsconfig.app.json` (`-p` 없이는 0파일 검사 거짓 녹색).
- 커밋 메시지·주석 한국어, 태스크 단위 원자 커밋.

## 실행 모델 · 터미널

프론트 단일 도메인 + 순차 의존(1→2→4, 3→4)이라 **터미널 1개**. 브랜치는 **현재 작업 중인 `feature/concept-sets`를 그대로 이어 쓴다** — 선행 8커밋(컨셉 파생·조합기 오버라이드·`SetTabs`)이 이 브랜치에만 있고 `/sets`가 그것을 직접 소비하므로, 새 브랜치를 따면 기점이 갈린다. worktree도 현재 것(`.claude/worktrees/뷰티보이-컨셉세트`)을 그대로 쓴다.

- 오케스트레이터: **opus** — 태스크마다 서브에이전트 스폰, 사이사이 리뷰.
- 태스크 실행 서브에이전트: **sonnet** — 모델 배분 예외 3종 미해당(궁합 엔진은 소비만 한다).

### 이미 이 worktree에 있는 세션이 이어서 실행하는 경우

worktree 생성 단계가 필요 없다. 아래만 확인하고 Task 1로 들어간다:

```
pwd            # .claude/worktrees/뷰티보이-컨셉세트 여야 한다
git branch --show-current   # feature/concept-sets
git status     # 깨끗해야 한다
git log --oneline -1        # 99cb021 docs(spec): 세트 비교 페이지 /sets 설계
```

### 새 터미널에서 시작하는 경우 (그대로 붙여넣기)

```
[1단계 — 작업 공간 확인] 다른 무엇보다 먼저 이것부터 해라.
이 작업은 **기존 worktree와 기존 브랜치를 이어서** 쓴다. 새로 만들지 마라.
  EnterWorktree 도구에 path로 .claude/worktrees/뷰티보이-컨셉세트 를 넘겨 세션을 그 안으로 옮겨라.
진입 후 아래를 확인하고, 하나라도 어긋나면 중단하고 보고해라:
  - pwd가 해당 worktree인지
  - git branch --show-current 가 feature/concept-sets 인지
  - git log --oneline -1 이 99cb021(docs(spec): 세트 비교 페이지 /sets 설계)인지
  - docs/superpowers/plans/2026-07-28-sets-page.md 와 DESIGN.md 가 존재하는지
  - git status가 깨끗한지
worktree가 없다면 중단하고 보고해라 — 새로 만들면 선행 8커밋이 없는 기점이 된다.

[2단계 — 실행]
superpowers:subagent-driven-development 스킬로
docs/superpowers/plans/2026-07-28-sets-page.md 를 태스크 단위로 실행해라.
태스크 실행 서브에이전트는 model=sonnet 으로 스폰하고, 너는 오케스트레이터로서
태스크 사이마다 테스트 통과·Files 목록 준수를 리뷰해라.
Task 5의 스크린샷은 경로를 보고서에 남기고, 너도 직접 열어 보고 판정해라.
```

---

### Task 1: 일괄 담기 로직을 공용 함수로 추출

**Files:**
- Create: `frontend/src/features/routine/addSetToCart.ts`
- Create: `frontend/src/features/routine/addSetToCart.test.ts`
- Modify: `frontend/src/pages/Main.tsx` (`handleAddAll` 본문만 — 공용 함수 호출로 교체)

**Interfaces:**
- Consumes: `frontend/src/components/routine/PickCard.tsx`의 `addPickToCart(queryClient, goodsNo): Promise<void>`
- Produces: `addSetToCart(queryClient: QueryClient, goodsNos: number[]): Promise<{ added: number; skipped: number }>` — Task 4가 `/sets`에서 사용

- [ ] **Step 1: 실패하는 테스트 작성**

`addSetToCart.test.ts` — 문구가 아니라 **집계와 부분 성공 보존**이 사양이다:

```ts
import { describe, expect, it, vi } from 'vitest';
import { QueryClient } from '@tanstack/react-query';
import * as pickCard from '../../components/routine/PickCard';
import { addSetToCart } from './addSetToCart';

function clientOf() {
  return new QueryClient({ defaultOptions: { queries: { retry: false } } });
}

describe('addSetToCart — 세트 일괄 담기 집계', () => {
  it('전량 성공하면 added가 개수와 같고 skipped는 0이다', async () => {
    const spy = vi.spyOn(pickCard, 'addPickToCart').mockResolvedValue(undefined);
    const result = await addSetToCart(clientOf(), [101, 202, 303]);
    expect(result).toEqual({ added: 3, skipped: 0 });
    expect(spy).toHaveBeenCalledTimes(3);
    spy.mockRestore();
  });

  it('일부가 실패해도 나머지는 담기고 skipped로 집계된다 (롤백하지 않는다)', async () => {
    const spy = vi
      .spyOn(pickCard, 'addPickToCart')
      .mockResolvedValueOnce(undefined)
      .mockRejectedValueOnce(new Error('품절'))
      .mockResolvedValueOnce(undefined);
    const result = await addSetToCart(clientOf(), [101, 202, 303]);
    expect(result).toEqual({ added: 2, skipped: 1 });
    expect(spy).toHaveBeenCalledTimes(3); // 실패 후에도 멈추지 않는다
    spy.mockRestore();
  });

  it('전량 실패하면 added가 0이다', async () => {
    const spy = vi.spyOn(pickCard, 'addPickToCart').mockRejectedValue(new Error('실패'));
    const result = await addSetToCart(clientOf(), [101, 202]);
    expect(result).toEqual({ added: 0, skipped: 2 });
    spy.mockRestore();
  });

  it('빈 배열이면 담기를 시도하지 않는다', async () => {
    const spy = vi.spyOn(pickCard, 'addPickToCart').mockResolvedValue(undefined);
    const result = await addSetToCart(clientOf(), []);
    expect(result).toEqual({ added: 0, skipped: 0 });
    expect(spy).not.toHaveBeenCalled();
    spy.mockRestore();
  });
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd frontend && npx vitest run src/features/routine/addSetToCart.test.ts`
Expected: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

판단이 있는 곳(부분 실패 정책)이라 전량 명세한다:

```ts
import type { QueryClient } from '@tanstack/react-query';
import { addPickToCart } from '../../components/routine/PickCard';

/**
 * 세트/루틴 일괄 담기 — 픽을 순차로 담고 **실패는 건너뛰고 집계로 돌려준다.**
 *
 * 전량 롤백하지 않는 이유: 장바구니는 편집 가능한 중간 상태라, 4개를 담아 두는 편이
 * "하나가 품절이라 아무것도 못 담았다"보다 항상 낫다(선행 설계 §4.3의 판단을 그대로 옮김).
 *
 * 문구는 화면이 정한다 — 이 함수는 결과만 돌려주고 토스트를 띄우지 않는다.
 * 순차로 도는 이유: 장바구니는 서버 상태라 동시 요청이 수량 병합 순서를 흔들 수 있다.
 */
export async function addSetToCart(
  queryClient: QueryClient,
  goodsNos: number[],
): Promise<{ added: number; skipped: number }> {
  let added = 0;
  for (const goodsNo of goodsNos) {
    try {
      await addPickToCart(queryClient, goodsNo);
      added += 1;
    } catch {
      // 품절·네트워크 실패 모두 이 픽만 건너뛴다.
    }
  }
  // 전량 실패해도 무효화한다 — 실패 원인이 서버 측 상태 변화일 수 있어, 실패를 이유로
  // 캐시를 낡은 채 두면 화면이 서버와 어긋난다. 추출 전 Main.tsx의 동작과도 같다.
  queryClient.invalidateQueries({ queryKey: ['cart'] });
  return { added, skipped: goodsNos.length - added };
}
```

- [ ] **Step 4: 통과 확인**

Run: `cd frontend && npx vitest run src/features/routine/addSetToCart.test.ts`
Expected: PASS 4건.

- [ ] **Step 5: `Main.tsx`의 `handleAddAll`을 공용 함수로 교체**

토스트 문구와 `addingAll` 로딩 상태는 **화면에 그대로 남기고**, 담기 루프만 위임한다:

```ts
async function handleAddAll() {
  setAddingAll(true);
  try {
    const { added, skipped } = await addSetToCart(
      queryClient,
      picks.map((pick) => pick.goodsNo),
    );
    if (added === 0) {
      toast('담지 못했어요. 잠시 후 다시 시도해 주세요', { tone: 'danger' });
    } else if (skipped === 0) {
      toast(`${added}개 담았어요`);
    } else {
      toast(`${added}개 담았어요 — ${skipped}개는 품절로 제외`);
    }
  } finally {
    setAddingAll(false);
  }
}
```

`addPickToCart` import가 더 이상 안 쓰이면 제거한다.

- [ ] **Step 6: 회귀 확인 — `/main`의 담기 동작이 그대로여야 한다**

Run: `cd frontend && npx vitest run src/pages/Main.test.tsx && npx tsc --noEmit -p tsconfig.app.json`
Expected: 전부 PASS. **기존 담기 테스트가 깨지면 추출이 틀린 것이다 — 테스트를 고치지 말고 구현을 고쳐라.**

- [ ] **Step 7: 커밋**

```bash
git add frontend/src/features/routine/addSetToCart.ts frontend/src/features/routine/addSetToCart.test.ts frontend/src/pages/Main.tsx
git commit -m "refactor(routine): 일괄 담기 로직을 addSetToCart 공용 함수로 추출"
```

---

### Task 2: `/main` 원복 + 히어로 링크 + 상단 여백

**Files:**
- Modify: `frontend/src/pages/Main.tsx`
- Modify: `frontend/src/pages/Main.css`
- Modify: `frontend/src/pages/Main.test.tsx` (세트 탭 관련 케이스 제거/교체 — 아래 명시)

**Interfaces:**
- Consumes: 없음 (제거 위주)
- Produces: `/main`에 `/sets`로 가는 링크. Task 4의 화면 흐름이 이것을 전제한다.

- [ ] **Step 1: 테스트를 새 사양으로 교체**

`Main.test.tsx`에서 **선행 구현이 추가한 세트 탭 케이스 3건을 삭제**하고, 아래 2건으로 대체한다.
(선행 구현 이전부터 있던 케이스는 절대 건드리지 마라.)

```ts
describe('세트 진입 링크 (/sets 스펙 §3)', () => {
  it('세트 탭이 더 이상 렌더되지 않는다', async () => {
    // given: 로그인 + me 응답(concerns 있음)
    await screen.findByText('클렌징'); // 화면이 뜬 뒤 판정
    expect(screen.queryAllByRole('tab')).toHaveLength(0);
    expect(screen.queryByText(/당신을 위한 세트/)).not.toBeInTheDocument();
  });

  it('히어로에 /sets로 가는 링크가 있다', async () => {
    const link = await screen.findByRole('link', { name: /세트 보러가기/ });
    expect(link).toHaveAttribute('href', '/sets');
  });
});
```

**추가로, 선행 구현 때 오버라이드 때문에 성립하지 않게 됐던 기존 콜드스타트 케이스가 다시
통과해야 한다** — `'콜드스타트면 5단계 픽이 전부 인기 1위이고 reason이 없다'` 계열. 이 케이스가
지금 skip/수정된 상태라면 **원래 단언으로 되돌려라**(오버라이드가 사라졌으므로 다시 성립한다).

- [ ] **Step 2: 실패 확인**

Run: `cd frontend && npx vitest run src/pages/Main.test.tsx`
Expected: 새 2건 FAIL(링크 없음 / 탭이 아직 있음).

- [ ] **Step 3: `Main.tsx`에서 세트 탭 제거**

지울 것: `SetTabs` import와 렌더, 스켈레톤 분기(`bb-set-tabs__skeleton*` 블록 전체),
`deriveSetConcepts` import와 `concepts` useMemo, `selectedSet` state, `meQuery`/`meSettled`/
`accessToken` 구독, `Skeleton`·`fetchMe`·`useQuery`·`SkinType` 등 이제 안 쓰는 import.

남길 것: `useComposer()` — **인자 없이** 호출한다(기본값 `null` → 프로필 고민 전체로 조합하던
원래 동작). 앵커 네비·5섹션·일괄 담기는 그대로.

`useComposer`의 `concernOverride` 파라미터 자체는 **지우지 마라** — Task 4가 쓴다.

- [ ] **Step 4: 히어로 링크 추가**

`bb-main__intro-inner` 안, `bb-main__lede` **아래**에 둔다:

```tsx
<Link className="bb-main__sets-link" to="/sets">
  컨셉별 세트 보러가기 →
</Link>
```

- 검정 밴드 위이므로 **흰 텍스트 링크**다. 채움 버튼(`Button` 컴포넌트)을 쓰지 마라 —
  DESIGN.md의 "화면당 검정 알약은 픽 카드 담기 하나" 규칙과 부딪힌다.
- 스타일은 Task 3이 DESIGN.md에 추가할 사양을 따른다. 이 태스크에서는 최소한의 배치
  (lede와의 간격, 흰색 글자, 밑줄 또는 화살표)만 `Main.css`에 넣고, 토큰만 참조한다.

- [ ] **Step 5: 상단 여백 해소**

`Main.css`의 `.bb-main__body`에 상단 여백을 준다. **검증 가능한 기준**: 히어로 밴드 하단
경계와 앵커 네비 상단 사이 흰 여백이 `--space-xl` 이상일 것(현재 0).

선행 구현이 넣은 `.bb-main__body > .bb-set-tabs`, `.bb-main__body > .bb-set-tabs__skeleton`
규칙은 이제 대상이 없으므로 **삭제**한다.

- [ ] **Step 6: 통과 확인**

Run: `cd frontend && npx vitest run src/pages/Main.test.tsx && npx tsc --noEmit -p tsconfig.app.json`
Expected: 전부 PASS.

- [ ] **Step 7: 커밋**

```bash
git add frontend/src/pages/Main.tsx frontend/src/pages/Main.css frontend/src/pages/Main.test.tsx
git commit -m "refactor(main): 세트 탭 제거하고 히어로에 /sets 링크 — 상단 여백 확보"
```

---

### Task 3: DESIGN.md `sets-page` 절 + `SetBand` 컴포넌트

**Files:**
- Modify: `DESIGN.md` (`set-tabs` 절을 `sets-page` 절로 교체 + 히어로 링크 한 줄)
- Create: `frontend/src/components/sets/SetBand.tsx`
- Create: `frontend/src/components/sets/SetBand.css`
- Delete: `frontend/src/components/routine/SetTabs.tsx`, `frontend/src/components/routine/SetTabs.css`

**Interfaces:**
- Consumes: Task 1의 `addSetToCart`는 쓰지 않는다(담기 실행은 부모가 한다) / `setConcepts.ts`의 `SetConcept` / `types/goods`의 `GoodsListItem` / `features/routine/steps`의 `ROUTINE_STEPS`
- Produces:
  ```ts
  interface SetBandProps {
    concept: SetConcept;
    letter: string;              // 'A' | 'B' | 'C' — setConcepts.ts의 SET_LETTERS에서 온다
    /** 단계 순서대로 5칸. 픽이 없는 단계는 null (그 칸만 비고 밴드는 유지) */
    picks: (GoodsListItem | null)[];
    /** 조합이 아직 확정되지 않았다 — 스켈레톤 렌더 */
    loading: boolean;
    onAddSet: () => void;
    adding: boolean;
  }
  export function SetBand(props: SetBandProps): JSX.Element;
  ```
  Task 4가 이 props 그대로 사용한다.

- [ ] **Step 1: DESIGN.md 교체 (스타일 코드보다 먼저 — 문서가 진실)**

`set-tabs` 절을 아래로 **교체**한다. 토큰명은 반드시 DESIGN.md의 Colors·Typography·Shapes 절과
대조해 **실존 토큰만** 쓰고, 초안에 없는 이름이면 대응 토큰으로 바꾸고 보고에 명시한다:

```markdown
**`sets-page`** — `/sets` 세트 비교 화면
- 페이지 제목 "당신을 위한 세트 3가지"(`{typography.heading-md}`) + 아이브로우 "PERSONAL SETS"
  (`{typography.eyebrow}`). 프로필이 없으면 제목 아래 안내 한 줄
  "프로필을 등록하면 맞춤 세트로 바뀌어요"(`{typography.meta}`) + 프로필 화면 텍스트 링크.
- **세트 밴드**가 세로로 3개 쌓인다. 밴드 사이는 1px `{colors.hairline-soft}` 구분선 +
  위아래 `{spacing.xl}`. 마지막 밴드 아래에는 선을 두지 않는다.
- **밴드 헤더 행**: 좌측 세트 제목 "세트 A · 브라이트닝"(`{typography.heading-sm}`),
  우측 액션 `[이 세트 5개 담기]`. 같은 줄에서 양끝 정렬한다 — 액션을 밴드 하단에 두지 않는다.
  액션은 `{button-ghost}`(검정 채움 금지 — 이 화면의 검정 알약은 상품 카드의 담기뿐이다).
- **밴드 본문**: 5칸 가로 한 줄, 칸 간격 `{spacing.lg}`. 각 칸은 단계 라벨("01 클렌징",
  `{typography.micro-caps}`) + 상품 카드. 대안 상품은 노출하지 않는다.
  픽이 없는 단계는 칸을 비우고 "추천할 상품이 없어요"(`{typography.meta}`)를 둔다 —
  칸을 접지 않는다(5칸 그리드가 밴드끼리 세로로 정렬되어야 비교가 된다).
- **로딩**: 헤더 제목·액션·5칸 전부 공용 `{Skeleton}`. 확정 후 높이가 흔들리지 않는다.
- 액센트(`signal-*`) 배경 금지. 개인화 여부를 색으로 구분하지 않는다.
- 모바일(768px 미만): 밴드 본문이 **가로 스크롤**(5칸은 한 줄에 안 들어간다). 헤더 행은
  제목과 액션이 세로로 접힌다. 한글이므로 `word-break: keep-all`.
```

히어로 절에도 한 줄 추가한다:

```markdown
- `/main` 히어로 밴드 안, lede 아래의 **세트 진입 링크** — 검정 밴드 위 흰 텍스트 링크
  (`{typography.link-sm}`), 화살표 `→`를 붙인다. **채움 버튼을 쓰지 않는다** —
  화면당 검정 알약은 픽 카드의 담기 하나여야 한다.
```

- [ ] **Step 2: DESIGN.md 커밋 (컴포넌트보다 먼저)**

```bash
git add DESIGN.md
git commit -m "docs(design): sets-page 절로 교체 — 세트 밴드 사양 + 히어로 진입 링크"
```

- [ ] **Step 3: `SetBand` 구현 + `SetTabs` 삭제**

표시 전용 컴포넌트라 시그니처 + 사양으로 충분하다:

- props는 Interfaces 블록 그대로. 내부 상태 없음 — 담기 실행·로딩 판정은 부모(Task 4) 소관.
- 마크업: `<section>` → 헤더 행(`<h2>세트 {letter} · {concept.label}</h2>` + 담기 `Button variant="ghost"`)
  → 본문(`<ol>` 5칸, 각 칸에 단계 라벨 + 카드).
- 단계 라벨은 `ROUTINE_STEPS`에서 가져온다(순서·개수의 유일한 출처). `picks` 길이가
  `ROUTINE_STEPS.length`와 다르면 렌더 계약 위반이므로, 그 전제를 코드 주석에 남긴다.
- 담기 버튼 접근명은 `이 세트 5개 담기`가 아니라 **실제 픽 개수**를 쓴다(품절 등으로 4개일 수
  있다). 즉 `이 세트 {픽 개수}개 담기`.
- 상품 카드는 기존 컴포넌트를 재사용한다 — `frontend/src/components/goods/` 아래에서
  목록용 카드를 찾아 쓰고, 없으면 `PickCard`를 참고하되 **대안·이유 문장은 렌더하지 않는다**.
  어느 것을 택했는지 보고서에 남겨라.
- `loading`이면 헤더·5칸을 공용 `Skeleton`(`ratio="auto"`)으로 렌더하고, **확정 상태와 같은 높이**를
  만든다. 계산을 보고서에 적어라.
- CSS는 방금 추가한 `sets-page` 절 토큰만 참조. 클래스는 `bb-set-band__*`.
- `SetTabs.tsx`/`SetTabs.css`를 삭제한다(Task 2에서 마지막 소비자가 사라졌다).
  `SetTabs`를 참조하는 곳이 남아 있지 않은지 `grep -rn "SetTabs" frontend/src`로 확인해라.

- [ ] **Step 4: 타입 게이트**

Run: `cd frontend && npx tsc --noEmit -p tsconfig.app.json`
Expected: PASS. (렌더 검증은 Task 4 테스트와 Task 5 스크린샷 소관.)

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/components/sets/SetBand.tsx frontend/src/components/sets/SetBand.css
git add -u frontend/src/components/routine/
git commit -m "feat(sets): SetBand 컴포넌트 — 헤더 행 액션 + 5단계 가로 한 줄, SetTabs 제거"
```

---

### Task 4: `/sets` 페이지 + 라우트 + 화면 테스트

**Files:**
- Create: `frontend/src/pages/Sets.tsx`
- Create: `frontend/src/pages/Sets.css`
- Create: `frontend/src/pages/Sets.test.tsx`
- Modify: `frontend/src/router.tsx` (라우트 1개 추가)

**Interfaces:**
- Consumes: Task 1 `addSetToCart`, Task 3 `SetBand`, `setConcepts.ts`의 `deriveSetConcepts`/`SET_LETTERS`, `useComposer`
- Produces: 없음 (최종 화면)

- [ ] **Step 1: 실패하는 테스트 작성**

`Sets.test.tsx` 신규. `Main.test.tsx`의 msw 핸들러·픽스처 패턴(단계별 풀 통제, authStore 로그인,
IntersectionObserver/matchMedia 스텁)을 참고해 같은 방식으로 세운다. 케이스와 단언(스펙 §7):

```ts
describe('/sets 세트 비교 페이지', () => {
  it('프로필 회원 — 밴드 3개가 렌더되고 제목이 "세트 A · <컨셉>" 형식이다', async () => {
    // given: 로그인 + me 응답 concerns=['pore','trouble','moisture']
    const bands = await screen.findAllByRole('heading', { level: 2 });
    expect(bands).toHaveLength(3);
    expect(bands[0]).toHaveTextContent('세트 A · 모공');
    expect(bands[1]).toHaveTextContent('세트 B · 트러블');
    expect(bands[2]).toHaveTextContent('세트 C · 보습');
  });

  it('각 밴드에 5단계가 순서대로 있다', async () => {
    // then: 첫 밴드 안에 '01 클렌징' … '05 선크림'이 ROUTINE_STEPS 순서로 존재
  });

  it('세트 차별화 — 세 밴드의 세럼 단계 픽이 서로 다르다', async () => {
    // given: 세럼(C001002) 풀에 pore/trouble/moisture 태그 상품을 각각 심는다
    // then: 세 밴드의 세럼 칸 상품명이 3개 모두 다르다
  });

  it('세트별 담기 — 그 세트의 픽만 담긴다 (다른 세트 것이 섞이지 않는다)', async () => {
    // when: 두 번째 밴드의 담기 버튼 클릭
    // then: 장바구니 POST 호출의 goodsNo 집합이 세트 B의 픽 집합과 정확히 일치한다
  });

  it('일부 품절이면 집계 문구가 나온다', async () => {
    // given: 픽 중 하나의 장바구니 POST가 실패
    // then: /2개 담았어요 — 1개는 품절로 제외/ 형태의 토스트
  });

  it('프로필 미입력 회원 — 폴백 3종 라벨과 등록 유도 문구가 보인다', async () => {
    // given: 로그인 + me 응답 concerns=[], skinType=null
    const bands = await screen.findAllByRole('heading', { level: 2 });
    expect(bands.map((b) => b.textContent)).toEqual([
      expect.stringContaining('모공'),
      expect.stringContaining('트러블'),
      expect.stringContaining('보습'),
    ]);
    expect(screen.getByText(/프로필을 등록하면 맞춤 세트로 바뀌어요/)).toBeInTheDocument();
  });
});
```

주석으로 남긴 given/when은 픽스처로 구체화하되, **명시된 단언은 그대로 유지**해라.

- [ ] **Step 2: 실패 확인**

Run: `cd frontend && npx vitest run src/pages/Sets.test.tsx`
Expected: 전건 FAIL (페이지 없음).

- [ ] **Step 3: `Sets.tsx` 구현**

훅 호출 규칙이 걸린 곳이라 핵심을 전량 명세한다:

```tsx
const accessToken = useAuthStore((s) => s.accessToken);
const meQuery = useQuery({ queryKey: ['me'], queryFn: fetchMe, enabled: !!accessToken });
// me가 확정되기 전에 컨셉을 정하면 프로필 도착 순간 밴드가 갈아엎어진다.
const meSettled = !accessToken || meQuery.isSuccess || meQuery.isError;
const concepts = useMemo(
  () =>
    deriveSetConcepts(
      meQuery.data?.concerns ?? [],
      (meQuery.data?.skinType ?? null) as SkinType | null,
    ),
  [meQuery.data],
);

// 훅은 루프에 넣을 수 없다. SET_COUNT가 3이라는 전제를 여기서 펼쳐 쓴다 —
// setConcepts.ts의 SET_COUNT를 바꾸면 이 줄들도 함께 늘려야 한다.
const setA = useComposer(meSettled ? concepts[0].slug : null);
const setB = useComposer(meSettled ? concepts[1].slug : null);
const setC = useComposer(meSettled ? concepts[2].slug : null);
const states = [setA, setB, setC];
```

밴드 렌더:

```tsx
{concepts.map((concept, index) => (
  <SetBand
    key={concept.slug}
    concept={concept}
    letter={SET_LETTERS[index]}
    picks={states[index].map((step) => step.composition?.pick ?? null)}
    loading={!meSettled || states[index].some((step) => step.composition === undefined)}
    adding={addingIndex === index}
    onAddSet={() => handleAddSet(index)}
  />
))}
```

담기 핸들러 — 문구는 `/main`과 같은 체계를 쓴다:

```tsx
const [addingIndex, setAddingIndex] = useState<number | null>(null);

async function handleAddSet(index: number) {
  setAddingIndex(index);
  try {
    const goodsNos = states[index]
      .map((step) => step.composition?.pick?.goodsNo)
      .filter((no): no is number => no !== undefined);
    const { added, skipped } = await addSetToCart(queryClient, goodsNos);
    if (added === 0) {
      toast('담지 못했어요. 잠시 후 다시 시도해 주세요', { tone: 'danger' });
    } else if (skipped === 0) {
      toast(`${added}개 담았어요`);
    } else {
      toast(`${added}개 담았어요 — ${skipped}개는 품절로 제외`);
    }
  } finally {
    setAddingIndex(null);
  }
}
```

폴백 안내는 `concepts.every((c) => !c.personalized)`일 때만 제목 아래에 렌더한다.

- [ ] **Step 4: 라우트 추가**

`router.tsx`의 `main` 라우트 **바로 뒤**에 같은 형태로 넣는다:

```tsx
{
  path: 'sets',
  element: (
    <RequireAuth>
      <Sets />
    </RequireAuth>
  ),
},
```

- [ ] **Step 5: 통과 확인**

Run: `cd frontend && npx vitest run src/pages/Sets.test.tsx`
Expected: 전건 PASS.

- [ ] **Step 6: 전체 게이트**

Run: `cd frontend && npx tsc --noEmit -p tsconfig.app.json && npm test`
Expected: 전부 PASS.

- [ ] **Step 7: 커밋**

```bash
git add frontend/src/pages/Sets.tsx frontend/src/pages/Sets.css frontend/src/pages/Sets.test.tsx frontend/src/router.tsx
git commit -m "feat(sets): /sets 세트 비교 페이지 — 밴드 3개 세로 나열, 세트별 담기"
```

---

### Task 5: 렌더 검증 (스크린샷 DoD)

**Files:** 수정 없음 (검증 전용 — 스크린샷은 커밋하지 않는다)

- [ ] **Step 1: 스택 기동**

이미 `beautyboy` compose 스택이 떠 있을 수 있다(포트 3000/8080/13306). 떠 있으면
`docker compose -p beautyboy up -d --build frontend`로 프론트만 이 코드로 교체하고,
없으면 README A절대로 전체를 올린다. **다른 세션 스택을 죽이지 마라.**
시드 계정: `dry@beautyboy.dev` / `seed1234!`

- [ ] **Step 2: 스크린샷 4장 — 찍고 직접 본다**

Playwright, 데스크톱 1280px. 워크트리 밖 스크래치 경로에 저장한다.

1. `main-hero-link.png` — `/main` 상단: 히어로 밴드 안 세트 링크 + **밴드 아래 여백이 확보돼
   앵커 네비까지 답답하지 않은지**. 이 장이 이번 변경의 핵심 동기다.
2. `sets-full.png` — `/sets` 전체(밴드 3개가 다 보이게, 필요하면 full-page 캡처).
3. `sets-band-header.png` — 밴드 하나를 확대: 제목과 담기 버튼이 **같은 줄 양끝**에 있는지.
4. `sets-fallback.png` — 프로필 미입력 회원(가입 시 2단계 건너뛰기)의 `/sets`.

주의: Playwright `click()`은 자동 스크롤한다 — 스크롤 위치가 중요한 장면은 `evaluate`로 클릭.

- [ ] **Step 3: 판정 체크리스트**

- `/main` 밴드 하단 ~ 앵커 네비 사이 여백이 `--space-xl` 이상으로 보이는가(답답함 해소)
- 히어로 링크가 검정 밴드 위에서 읽히는가, 채움 버튼이 아닌가
- 세 밴드의 5칸이 **세로로 정렬**되어 단계별 비교가 되는가
- 담기 버튼이 제목과 같은 줄 우측인가(하단 아님)
- 세 밴드의 세럼 칸 상품이 실제로 서로 다른가 — **세트 수렴이 눈에 보이는지**가 이 화면의 시험대다
- 한글 줄바꿈 잘림 없음, 액센트 배경 없음, 로딩→확정 높이 흔들림 없음

- [ ] **Step 4: 보고**

스크린샷 4장의 절대 경로 + 판정 결과를 오케스트레이터에 보고. 오케스트레이터도 직접 열어 판정한다.

---

## 머지

전 태스크 완료 + Task 5 판정 통과 후, 오케스트레이터가 최종 전체 리뷰를 돌리고
`superpowers:finishing-a-development-branch`로 `feature/concept-sets` → `main` 통합을 진행한다.
(선행 컨셉 세트 8커밋과 이번 변경이 같은 브랜치에 있으므로 한 번에 병합된다.)
